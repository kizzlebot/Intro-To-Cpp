#include <iostream>
#include <fstream>
using namespace std;
/*
    +boolean logic (conditional statement)
        -- true or false
    +compound boolean
        -- and statement
            -- this is true AND that is true
        -- or statement
            -- this is true OR that is true
---------------------------------------------------
    + example
    ((x > 10) or (y < 17)) and (z > 100) 
        -- x = 9, y = 15, z = 200
    ((x > 10) or (y < 17)) and (z > 100)
        F           T             T
           \     /            
              T         and       T 
                    
                      True



*/


int main(){
    int x;
    cout << "Enter int input: " ;
    cin >> x;
    

    /*
        AND - &&
        OR  - ||
    */
    if ((x > 10) && (x < 20)) {
        
    }

    if else (){
        
    }

    else{
        
    }
    
    
    return 0;
}
